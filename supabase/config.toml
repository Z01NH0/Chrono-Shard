project_id = "favrbugywlamhitktocy"

[functions.game-api]
# Chamadas do jogo usam a sessão anônima/autenticada no Authorization header.
# O wrapper withSupabase({ auth: 'user' }) faz a segunda validação dentro da função.
verify_jwt = true
